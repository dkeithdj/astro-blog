---
type Props = {
  text: string;
  link: string;
};
const { text, link } = Astro.props;
---

<a
  href={link}
  class="hover:text-teal-800 transition-all text-teal-600 cursor-pointer"
  >{text}</a
>
